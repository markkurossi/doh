# DNS over HTTPS Server

Go implementation of the [RFC
8484](https://www.rfc-editor.org/rfc/rfc8484) DNS over HTTPS
server. The server is implemented as GCP cloud function and it
supports optional DNS query filtering based on pre-configured allow
and block lists.
